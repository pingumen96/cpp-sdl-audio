#include "InputEnabledScene.h"
#include <iostream>

namespace game {
    // Implementation file for InputEnabledScene
    // Currently all methods are inline in the header
    // Add non-inline implementations here if needed in the future
}
