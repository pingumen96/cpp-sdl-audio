#include "InputState.h"

namespace ecs {
    // Implementation file for InputState
    // Currently all methods are inline in the header for simplicity
    // Add non-inline implementations here if needed in the future
}
