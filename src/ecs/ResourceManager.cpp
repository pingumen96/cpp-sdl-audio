#include "ResourceManager.h"

namespace ecs {
    // Implementation file for ResourceManager
    // Currently all methods are template-based and inline in the header
    // Add non-template implementations here if needed in the future
}
